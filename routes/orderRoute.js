import express from "express"
const router = express.Router();

router.post("/orderPlaced",async (req,res)=>{

    const {token,totakAmount,currentUser,cartItems}=req.body

    try {
        
    } catch (error) {
        
    }


})
